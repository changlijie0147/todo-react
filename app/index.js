import React from "react";
import ReactDOM from "react-dom";
import Todos from "./components/main";
import './index.scss';

// ReactDOM.render(< Todos / >, document.getElementById("content"));
ReactDOM.render(
  <Todos/>, document.getElementById('root'))